-- IGNORE
-- TODO: should be EXPECT 34, need to handle casing on literals/variables (not just constructors)

main = fib 8

fib n =
  case n of
    0 -> 1
    1 -> 1
    n -> fib (n - 1) + fib (n - 2)))
