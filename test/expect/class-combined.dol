-- EXPECT 18

class Add a where
  add :: a -> a -> a

instance Add Int where
  add x y = x + y

class Mul a where
  mul :: a -> a -> a

instance Mul Int where
  mul x y = x * y

double :: Add a => a -> a
double x = add x x

main = mul (double 3) 3
