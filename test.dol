pair x y f = f x y; fst p = p K;
snd p = p K1;
f x y =
  let a = pair x b
    , b = pair y a
  in
    fst (snd (snd (snd a)));
main = f 3 4;

-- id x = x;
-- main = twice twice id 3;

/* id x = x; */
/* main = twice twice twice id 3; */
