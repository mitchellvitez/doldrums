three = if (2 == 3 || 3 < 4) 3 7;
four = ((1 + 6 - 2) * 4) / 5;

pair x y f = f x y;
fst p = p K;
snd p = p K1;
id y = (\x. x) y;

f x y =
  let b = pair y 7
    , a = pair x b
  in
    fst $ snd $ snd a;

main = f three (id four);
